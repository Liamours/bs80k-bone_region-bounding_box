# Glossary

- Raw image: the full source image before any cropping
- Cropped image: a sub region cut from the raw image, treated here as a template to locate
- Bounding box: (x, y, width, height) marking where the cropped image sits inside the raw image
- Template matching: sliding a smaller image, the template, over a larger image to find the best matching location
